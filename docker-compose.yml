name: pravburo-reff-bounty

services:
  pravburo-reff-bounty:
    build: .
    env_file: .env
    network_mode: host
    command: ["uv", "run", "--no-sync", "uvicorn", "src.main:app", "--host", "127.0.0.1", "--port", "${BOUNTY_PORT:-8041}"]
    restart: unless-stopped
